# MQTT Worker — Fleet Tracker India
# Points at our own EMQX broker (plain TCP, internal network)
# DO NOT use the HSL public feed anymore — we have our own broker.

MQTT_TOPIC=trucks/#
MQTT_BROKER=emqx
MQTT_PORT=1883
MQTT_N_WORKERS=10
