API_HOST=localhost

# Public MQTT endpoint written into /cdn/fleet-config.json by the frontend
# container. Set this to your VPS IP, domain, or tunnel host for phones.
PUBLIC_MQTT_HOST=localhost
PUBLIC_MQTT_PORT=1883
